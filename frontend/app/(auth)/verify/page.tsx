import React from 'react';
import {VerifyPage} from "@/page-s/Verify";

const Verify = () => {
    return (
        <section>
            <VerifyPage />
        </section>
    );
};

export default Verify;